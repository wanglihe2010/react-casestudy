export default class Sku{
  constructor(id, pid, price, stock, img, option) {
    this.id = id;
    this.pid = pid;
    this.price = price;
    this.stock = stock;
    this.img = img;
    this.option = option;
  }
}